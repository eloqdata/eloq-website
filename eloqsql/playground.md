# Playground

Run a EloqSQL Playground locally.

Install eloqctl

```shell
curl -fsSL https://raw.githubusercontent.com/eloqdata/eloqctl/main/install.sh | sh
```

Start to play with EloqSQL

```shell
eloqctl demo eloq-sql
```
